-- Program updates Johns score to 10
-- UPDATE statement modifies data in tables
-- within database chosen.
-- WHERE CLAUSE is used to filter data.

UPDATE second_table
SET score = 10
WHERE name = "John";
