-- Program list score and name records in second_table
-- in descending order (Top first).
-- ORDER BY CLAUSE is used to sort the given records.

SELECT score, name
FROM second_table
ORDER BY score DESC;
